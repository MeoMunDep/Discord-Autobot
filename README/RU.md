# 🤖 Discord Automation Bot от @MeoMunDep

<p align="center">
  <img src="https://github.com/MeoMunDep/Discord-Autobot/blob/main/IMAGE/DISCORD_MEOMUNDEP_CLI.png?raw=true" width="600" />
</p>

---

## 🌐 Обзор

Этот бот автоматизирует взаимодействия в **Discord**, включая отправку сообщений, вступление/выход из серверов и обработку сообщений с использованием **нескольких аккаунтов** и **поддержкой прокси**.

---

## 🚀 Возможности

* 💬 **Авто-отправка сообщений** — отправляет сообщения в указанные каналы с настраиваемыми задержками
* 🔗 **Система отправки постов (Submit Channel)** — выделенная система для подачи постов X/Twitter
* ➕ **Авто-вступление в серверы** — присоединяется по инвайт-ссылкам
* ➖ **Авто-выход из серверов** — выходит из указанных серверов
* 🌍 **Поддержка прокси** — HTTP(S) и SOCKS5 прокси
* 🗑️ **Авто-удаление сообщений** — удаляет отправленные сообщения после настраиваемой задержки
* 🌐 **Мульти-язык** — настраиваемый язык сообщений
* 📕 **Лог ошибок** — сохраняет ID серверов/каналов, где произошли ошибки
* 🤖 **AI-ответы** — генерация ответов с помощью AI-провайдеров (Groq, OpenRouter, Gemini, Poe, Grok)
* 🖼️ **Генерация изображений** — генерирует и отправляет AI-изображения в каналы через Freepik API (fallback — Pollinations.ai)
* 🎛️ **Управление серверами** — включение/отключение отдельных серверов без удаления конфигурации
* ⚙️ **Два режима работы** — нормальный режим (полный цикл) или submit-only режим
* 🎮 **Поддержка голосовых каналов** — подключается и удерживает присутствие в голосовых каналах
* 📊 **Интерактивное управление** — встроенное CLI-меню для управления серверами, задержками и ошибками
* 🚀 **Флаги CLI** — переопределение настроек через аргументы командной строки

---

<details>
<summary><strong>🛠️ Установка</strong></summary>

### 📋 Требования

* **Node.js** `v22.11.0`
* **npm** `v10.9.0`

📦 Скачать: [Node.js & npm](https://t.me/KeoAirDropFreeNe/257/1462)

---

### 📥 Шаги

1. **Клонировать репозиторий:**

   ```bash
   git clone https://github.com/MeoMunDep/Discord-Autobot.git
   cd "discord-autobot"
   ```

2. **Установить зависимости:**

   ```bash
   npm install
   ```

   ⚠️ Если на Windows появляется ошибка *Execution Policy*:

   ```bash
   Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
   ```

   Затем повторите:

   ```bash
   npm install
   ```

3. **Настроить бота:**
   Отредактируйте файл `configs.json` под свои нужды.

4. **Подготовить входные файлы:**

   * 📄 `tokens.txt` — Discord-токены (по одному на строку)
   * 🆔 `servers.json` — информация о серверах (guild IDs, invite codes, конфигурация каналов — см. ниже)
   * 🌐 `proxies.txt` — прокси (опционально, по одному на токен)
   * 📜 `messages.yaml` — сообщения (мультиязычные)
   * 🗂️ `error-channels.json` — хранит неудачные каналы/серверы (генерируется/обновляется автоматически)

📌 Полезные ссылки:

* 🔑 Получить токены: [Здесь](https://t.me/KeoAirDropFreeNee/1570)
* 💬 Получить chat ID: [Здесь](https://t.me/KeoAirDropFreeNee/1569)
* 📡 Получить channel/server ID: [Здесь](https://t.me/KeoAirDropFreeNee/1676)

</details>

---

<details>
<summary><strong>⚙️ Конфигурация (`configs.json`)</strong></summary>

| ⚙️ Параметр                    | 📘 Описание                                                                  |
| ------------------------------ | ---------------------------------------------------------------------------- |
| `max_accounts_at_once`         | Количество аккаунтов, работающих одновременно                                |
| `delay_between_accounts`       | Задержка (в секундах) между запуском каждого аккаунта                        |
| `delay_each_channel`           | Диапазон [min, max] задержки (в сек.) между разными каналами                 |
| `delay_each_chat`              | Диапазон [min, max] задержки (в сек.) между сообщениями в том же канале      |
| `time_to_restart_all_accounts` | Время (в сек.) до перезапуска всех аккаунтов                                 |
| `chat_language`                | Язык для авто-сгенерированных сообщений                                      |
| `ask_before_auto_chat`         | Спрашивать режим (submit_only/normal) перед очередным циклом                 |
| `auto_chat`                    | Режим по умолчанию: true = normal, false = submit_only                       |
| `submit_x_url`                 | Включить/выключить отправку X/Twitter ссылок в обычных каналах               |
| `join_server`                  | Включить/выключить автоприсоединение к серверам                              |
| `leave_server`                 | Включить/выключить авто-выход из серверов                                    |
| `delete_message`               | Удалять отправленные сообщения после задержки                                |
| `enable_replies`               | Включить/выключить ответы на сообщения                                       |
| `enable_reactions`             | Включить/выключить реакции на сообщения                                      |
| `reaction_chance`              | Вероятность (0–1) реакции на сообщение                                       |
| `enable_ai_replies`            | Включить/выключить AI-ответы                                                 |
| `ai_reply_chance`              | Вероятность (0–1) использования AI для ответа                                |
| `ai_providers.*`               | API-ключи (массивы) для AI-провайдеров (Groq, Gemini, Poe, OpenRouter, Grok) |
| `image_generation_providers.*` | API-ключи (массивы) для провайдеров генерации изображений (Freepik)          |
| `voice_chat.enabled`           | Включить/выключить подключение к голосовому каналу                           |
| `voice_chat.server_id`         | Guild ID для голосового канала                                               |
| `voice_chat.channel_id`        | ID голосового канала                                                         |
| `voice_chat.user_id`           | Ваш user ID для голосового подключения                                       |

---

### 🧾 Пример `configs.json`

```json
{
  "max_accounts_at_once": 1,
  "delay_between_accounts": 15,
  "delay_each_channel": [5, 7],
  "delay_each_chat": [10, 15],
  "time_to_restart_all_accounts": 963,

  "ask_before_auto_chat": false,
  "auto_chat": true,
  "submit_x_url": false,
  "delete_message": false,
  
  "join_server": false,
  "leave_server": false,

  "enable_replies": true,
  "ai_reply_chance": 0.7,

  "enable_reactions": true,
  "reaction_chance": 0.7,
  
  "enable_ai_replies": true,
  "ai_providers": {
    "groq": ["gsk_xxxxxxxxxxxxxxxxxxxxx"],
    "openrouter": [
      "sk-or-v1-xxxxxxxxxxxxx",
      "sk-or-v1-yyyyyyyyyyyyy"
    ],
    "gemini": [
      "AIzaSyXXXXXXXXXXXXXXX",
      "AIzaSyYYYYYYYYYYYYYYY",
      "AIzaSyZZZZZZZZZZZZZZZ"
    ],
    "poe": [""],
    "grok": ["xai-xxxxxxxxxxxxxxxxxxxxx"]
  },

  "image_generation_providers": {
    "freepik": [
      "fpk_xxxxxxxxxxxxxxxxxxxxx",
      "fpk_yyyyyyyyyyyyyyyyyyyyy"
    ]
  },

  "voice_chat": {
    "enabled": false,
    "server_id": "",
    "channel_id": "",
    "user_id": ""
  }
}
```

---

### 🔑 Несколько API-ключей

Бот поддерживает **несколько API-ключей** для каждого провайдера. Для каждого запроса случайным образом выбирается один ключ из массива:

```json
"ai_providers": {
  "groq": ["key1", "key2", "key3"],
  "gemini": ["key1", "key2"]
}
```

Преимущества:

* **Распределение нагрузки** между аккаунтами
* **Управление лимитами** запросов
* **Надёжность** — при падении одного ключа используются другие

---

<details>
<summary><strong>🔑 GROQ API Key</strong></summary>

[Создать ключ](https://console.groq.com/keys)

<p align="center">
  <img src="https://github.com/MeoMunDep/Discord-Autobot/blob/main/IMAGE/GROQ_API_KEY.png?raw=true" width="600" />
</p>

</details>

<details>
<summary><strong>🔑 GROK API Key</strong></summary>

[Создать ключ](https://console.x.ai/api-keys)

**Примечание:** Grok API использует OpenAI-совместимый формат. Получите API-ключ в консоли X.AI.

</details>

<details>
<summary><strong>🔑 GEMINI API Key</strong></summary>

[Создать ключ](https://aistudio.google.com/app/apikey)

<p align="center">
  <img src="https://github.com/MeoMunDep/Discord-Autobot/blob/main/IMAGE/GEMINI_API_KEY.png?raw=true" width="600" />
</p>

</details>

<details>
<summary><strong>🔑 OPENROUTER API Key</strong></summary>

[Создать ключ](https://openrouter.ai/settings/keys)

<p align="center">
  <img src="https://github.com/MeoMunDep/Discord-Autobot/blob/main/IMAGE/OPENROUTER_API_KEY.png?raw=true" width="600" />
</p>

</details>

<details>
<summary><strong>🔑 POE API Key</strong></summary>

[Создать ключ](https://poe.com/api_key)

<p align="center">
  <img src="https://github.com/MeoMunDep/Discord-Autobot/blob/main/IMAGE/POE_API_KEY.png?raw=true" width="600" />
</p>

</details>

<details>
<summary><strong>🔑 FREEPIK API Key</strong></summary>

[Создать ключ](https://api.freepik.com/v1/auth/create)

<p align="center">
  <img src="https://github.com/MeoMunDep/Discord-Autobot/blob/main/IMAGE/FREEPIK_API_KEY.png?raw=true" width="600" />
</p>

</details>

</details>

---

<details>
<summary><strong>🗂️ Структура `servers.json` (ПОДРОБНО)</strong></summary>

Файл `servers.json` — **ядро конфигурации** бота. В нём задаётся, с какими серверами работать и какие действия выполнять в каждом канале.

---

## 📚 Общая структура

Файл — **JSON-массив**, содержащий объекты серверов. Каждый объект описывает один Discord-сервер.

```json
[
  {
    "name": "Server Name",
    "guild_id": "1234567890123456789",
    "invite_id": "inviteCode",
    "enabled": true,
    "channels": {
      "submit_chat": { ... },
      "raw_chat": { ... },
      "command_chat": { ... },
      "image_chat": { ... }
    }
  }
]
```

---

## 🔑 Поля и объяснения

### 1️⃣ **`name`** (String - Обязательно)

* Дружественное имя сервера (для вашего удобства).
* Пример: `"Curious"`, `"My Gaming Server"`, `"Crypto Community"`.

### 2️⃣ **`guild_id`** (String - Обязательно)

* Уникальный ID сервера Discord.
* Как получить: [Руководство](https://t.me/KeoAirDropFreeNee/1676)
* Пример: `"1417067849926705235"`

### 3️⃣ **`invite_id`** (String - Обязательно)

* Код приглашения для входа на сервер.
* Пример: `"altiuslabs"` (от discord.gg/altiuslabs)
* Специальные значения:

  * `""` (пустая строка) — не вступать в сервер
  * `"none"` — не вступать
  * `"unknown"` — не вступать

### 4️⃣ **`enabled`** (Boolean - Обязательно)

* Управляет обработкой сервера ботом.

  * `true` — бот будет обрабатывать сервер
  * `false` — бот пропустит сервер

### 5️⃣ **`channels`** (Object - Обязательно)

Здесь задаются действия по каналам. Имеются **четыре типа** каналов:

---

## 🗨️ Типы каналов

### 🔗 **`submit_chat`** — Отправка постов X/Twitter

**Назначение:** Канал для отправки ссылок X/Twitter с настраиваемыми задержками.

**Формат:**

```json
"submit_chat": {
  "CHANNEL_ID": {
    "message": "https://x.com/yourpost",
    "delay": [6000, 12000]
  }
}
```

**Поля:**

* `message` (String): URL X/Twitter для отправки
* `delay` (Array): [min, max] cooldown в секундах перед следующей отправкой
* `enabled` (Boolean, Optional): `false` — отключить конкретный канал

**Пример:**

```json
"submit_chat": {
  "1380138116505141288": {
    "message": "https://x.com/MeoMunDep/status/123456",
    "delay": [6000, 12000]
  },
  "1441334930268225637": {
    "message": "https://x.com/CryptoProject/status/987654321",
    "delay": [3600, 7200],
    "enabled": false
  }
}
```

**Поведение:**

* Каналы `submit_chat` обрабатываются в **submit-only** режиме или в нормальном режиме.
* Каждый канал имеет отдельный таймер cooldown.
* Если `submit_x_url: false` в `configs.json`, URL в обычных каналах игнорируются.

---

### 📝 **`raw_chat`** — Текстовые сообщения

Отправка обычных текстовых сообщений.

**Формат (Простой):**

```json
"raw_chat": {
  "CHANNEL_ID": "MESSAGE_TO_SEND"
}
```

**Формат (Расширенный):**

```json
"raw_chat": {
  "CHANNEL_ID": {
    "message": "MESSAGE_TO_SEND",
    "delay": [60, 120],
    "enabled": true
  }
}
```

**Специальные значения:**

* `"__random_message"` — бот возьмёт случайное сообщение из AI или `messages.yaml`
* `"[AI] Your instruction"` — бот использует AI с вашей инструкцией
* Любой конкретный текст — бот отправит ровно его (напр., `"gm"`, `"Hello!"`)

**Пример:**

```json
"raw_chat": {
  "1417068171243815002": {
    "message": "[AI] Respond to random messages",
    "delay": [60, 120]
  },
  "1417084328504197250": {
    "message": "gm",
    "delay": [6000, 12000]
  },
  "9876543210987654321": "__random_message"
}
```

**Поведение:**

* В канале `1417068171243815002` — бот использует AI с инструкцией «Respond to random messages»
* В канале `1417084328504197250` — бот отправляет «gm» каждые 6000–12000 секунд
* В канале `9876543210987654321` — бот отправляет случайное AI-сообщение

---

### 🤖 **`command_chat`** — Команды ботов

Отправка слеш-команд или команд-ботов в канал.

**Формат:**

```json
"command_chat": {
  "CHANNEL_ID": {
    "command": "COMMAND_TO_EXECUTE",
    "delay": [60, 120]
  }
}
```

**Пример:**

```json
"command_chat": {
  "1234567890123456789": {
    "command": "/claim daily",
    "delay": [86400, 86500]
  },
  "9876543210987654321": {
    "command": "!rank",
    "delay": [600, 1200]
  }
}
```

---

### 🖼️ **`image_chat`** — AI-изображения

Генерация и отправка AI-изображений в канал через Freepik API (fallback — Pollinations.ai).

**Формат:**

```json
"image_chat": {
  "CHANNEL_ID": {
    "prompt": "IMAGE_GENERATION_PROMPT",
    "delay": [60, 120]
  }
}
```

**Пример:**

```json
"image_chat": {
  "1435601397591703675": {
    "prompt": "Airdrop project with 'Curious'",
    "delay": [7200, 10000]
  },
  "9876543210987654321": {
    "prompt": "Beautiful sunset over mountains",
    "delay": [3600, 7200]
  }
}
```

**Требования:**

* Нужен `freepik` API ключ в `configs.json`
* Если Freepik недоступен или ключа нет — бот автоматически использует Pollinations.ai (бесплатно)

---

## 📋 Полный пример

```json
[
  {
    "name": "Curious",
    "guild_id": "1417067849926705235",
    "invite_id": "curious",
    "enabled": true,
    "channels": {
      "submit_chat": {
        "1417080821852147732": {
          "message": "https://x.com/Curious/status/123456",
          "delay": [6000, 12000]
        }
      },
      "raw_chat": {
        "1417068171243815002": {
          "message": "[AI] Respond to random messages",
          "delay": [60, 120]
        },
        "1417084328504197250": {
          "message": "gcurious",
          "delay": [600, 1200]
        }
      },
      "command_chat": {
        "1417068171243815003": {
          "command": "!rank",
          "delay": [600, 1200]
        }
      },
      "image_chat": {
        "1435601397591703675": {
          "prompt": "Airdrop project with 'CURIOUS'",
          "delay": [60, 120]
        }
      }
    }
  },
  {
    "name": "Disabled Server (Not Active)",
    "guild_id": "5555555555555555555",
    "invite_id": "oldserver",
    "enabled": false,
    "channels": {
      "submit_chat": {},
      "raw_chat": {
        "3333333333333333333": {
          "message": "test message",
          "delay": [60, 120]
        }
      },
      "command_chat": {},
      "image_chat": {}
    }
  }
]
```

---

## ⏱️ Система задержек (Delay System)

Бот использует **умную систему cooldown**, чтобы избежать спама и превышения лимитов:

### 📊 Приоритет задержек (от более высокого к более низкому):

1. **Задержка, заданная для канала** (в `servers.json`)

   ```json
   "1234567890": {
     "message": "gm",
     "delay": [300, 600]  ← это имеет приоритет
   }
   ```

2. **Глобальная `delay_each_chat`** (в `configs.json`)

   ```json
   "delay_each_chat": [10, 15]  ← используется как запасной вариант
   ```

### 🔄 Как это работает:

* Каждый канал имеет **независимый таймер cooldown**
* Таймер запускается после **успешной отправки сообщения**
* Бот проверяет, прошёл ли cooldown, прежде чем отправлять следующее сообщение
* Если cooldown активен — бот пропускает канал и логирует оставшееся время
* Cooldown’ы **постоянно сохраняются** в `chat-timestamps.json`

### 💡 Рекомендации:

* Используйте длинные задержки (3600–7200 с) для каналов с низкой активностью
* Для каналов с высокой активностью подойдут короткие задержки (60–300 с)
* Для `submit_chat` обычно используют 6000–12000 с
* Для генерации изображений рекомендованы большие интервалы (7200–10000 с) из-за стоимости API

---

## 💡 Советы и лучшие практики

### ✅ ДЕЛАТЬ:

* Давать понятные имена серверам для удобства
* Отключать сервер через `enabled: false`, а не удалять запись
* Отключать отдельные каналы через `enabled: false`
* Задавать задержки на уровне каналов для гибкого контроля
* Использовать формат `"[AI] instruction"` для кастомных AI-ответов
* Использовать `submit_chat` для X-постов, а не обычные каналы
* Тестировать с одним сервером, прежде чем добавлять много

### ❌ НЕ ДЕЛАТЬ:

* Не удаляйте записи серверов — лучше временно отключите (`enabled: false`)
* Не забывайте добавлять задержки (по умолчанию бот использует запасные значения, но лучше задать явно)
* Не ставьте очень короткие задержки (< 60 с) — риск бана/лимитов
* Не помещайте `tokens.txt` в общедоступные места и не делитесь им
* Не забывайте про корректный JSON — используйте валидатор, если сомневаетесь

---

## 🔍 Как найти Channel IDs

1. Включите Developer Mode в Discord (Settings → Advanced → Developer Mode)
2. Правый клик по каналу → Copy Channel ID
3. Вставьте ID в `servers.json`

[Визуальное руководство](https://t.me/KeoAirDropFreeNee/1676)

---

## 🐛 Устранение неполадок

| Проблема                             | Решение                                                                |
| ------------------------------------ | ---------------------------------------------------------------------- |
| Бот пропускает сервер                | Проверьте `enabled: true` у сервера                                    |
| Бот пропускает канал                 | Проверьте, не стоит ли `enabled: false` у канала                       |
| Бот не входит в сервер               | Проверьте `invite_id` и `join_server: true` в `configs.json`           |
| Бот не отправляет сообщения          | Проверьте корректность ID каналов и `auto_chat: true`                  |
| Сообщения отправляются слишком часто | Увеличьте `delay` в конфигурации каналов                               |
| Изображения не генерируются          | Проверьте Freepik API key в `configs.json` (или fallback используется) |
| Submit-каналы не работают            | Убедитесь, что запускаете в нужном режиме (submit-only или normal)     |
| Синтаксическая ошибка JSON           | Используйте валидатор (например, jsonlint.com)                         |

</details>

---

<details>
<summary><strong>▶️ Использование</strong></summary>

### 📌 Запуск бота

**Базовый запуск:**

```bash
node index_meomundep.js
```

**С флагами CLI:**

```bash
# Форсировать submit-only режим (только submit_chat)
node index_meomundep.js --submit
node index_meomundep.js -s

# Форсировать normal режим (полный поток: join, chat, leave)
node index_meomundep.js --chat
node index_meomundep.js -c

# Показать помощь
node index_meomundep.js --help
node index_meomundep.js -h
```

### 🎮 Режимы работы

#### 1️⃣ **Normal Mode** (Полный поток)

* Вступает в серверы (если `join_server: true`)
* Обрабатывает **все** типы каналов: `submit_chat`, `raw_chat`, `command_chat`, `image_chat`
* Выходит из серверов (если `leave_server: true`)
* Использует AI для генерации сообщений

**Активация:**

* CLI: `node index_meomundep.js --chat`
* Конфиг: `"auto_chat": true` (по умолчанию)
* В рантайме: выберите опцию `2` при запросе (если `ask_before_auto_chat: true`)

#### 2️⃣ **Submit-Only Mode**

* Пропускает join/leave
* Обрабатывает **только** `submit_chat` каналы
* Игнорирует `raw_chat`, `command_chat`, `image_chat`
* Хорош для задач отправки X/Twitter ссылок

**Активация:**

* CLI: `node index_meomundep.js --submit`
* Конфиг: `"auto_chat": false`
* В рантайме: выберите опцию `1` при запросе (если `ask_before_auto_chat: true`)

### 🔄 Приоритет режимов (от наивысшего к низшему):

1. **Флаги CLI** (`--submit` или `--chat`)
2. **Выбор пользователя** (если `ask_before_auto_chat: true`)
3. **Значение в конфиге** (`auto_chat`)

### 📊 Интерактивное управление

Встроенная система управления доступна из:

```bash
node index_meomundep.js
# Затем выберите "Server Manager" в главном меню
```

**Возможности управления:**

🖥️ **Главное меню:**

1. Управление серверами (вкл/выкл)
2. Управление задержками (добавить/показать)
3. Управление submit (X посты)
4. Управление ошибочными каналами
5. Выход

📡 **Управление серверами:**

* Включить/отключить все сервера
* Включить/отключить конкретные серверы
* Включить только один сервер (отключив все остальные)
* Показать обзор статусов серверов

⏱️ **Управление задержками:**

* Добавить дефолтные задержки для каналов без них
* Добавить кастомные задержки
* Показать статистику задержек
* Вывести уникальные значения задержек

🔗 **Управление submit:**

* Показать все submit-каналы
* Добавить новый submit-канал
* Редактировать URL для submit
* Удалить submit-канал
* Показать статистику submit

🚫 **Управление ошибочными каналами:**

* Просмотреть все ошибочные каналы по токену
* Удалить конкретные записи
* Удалить все записи токена
* Очистить логи неудач

### ⬆️ Обновление бота

```bash
git pull
npm install
```

---

### 🔍 Как работает (кратко)

1. Читает `configs.json`
2. Проверяет флаги CLI (`--submit`, `--chat`, `--help`)
3. Спрашивает режим (если `ask_before_auto_chat: true` и флаг не задан)
4. Определяет режим работы (submit-only или normal)
5. Логинится используя токены из `tokens.txt`
6. Применяет прокси для каждого аккаунта (если есть `proxies.txt`)
7. Обрабатывает каждый сервер из `servers.json`:

   * Проверяет `enabled: true`
   * Проверяет, что канал не `enabled: false`
   * Проверяет cooldown перед отправкой
   * **В normal режиме:**

     * Вступает в сервер (по `invite_id`) если `join_server: true`
     * Обрабатывает все типы каналов
     * Выходит из сервера если `leave_server: true`
   * **В submit-only режиме:**

     * Обрабатывает только `submit_chat`
     * Пропускает join/leave и другие каналы
8. Логирует каждое событие с временными метками и цветами
9. Сохраняет таймстампы для предотвращения дублирования
10. Ожидает заданные задержки между действиями
11. Повторяет цикл через `time_to_restart_all_accounts` секунд

---

### 📝 Логи и таймстампы

* Каждый лог содержит временную метку
* Формат метки зависит от `chat_language`
* Цветовая схема логов:

  * 🟢 Успех
  * 🔴 Ошибка
  * 🟡 Предупреждение
  * 🔵 Информация
  * 🟣 Обработка
  * 🔶 AI/действия провайдера

---

### 🎙️ Поддержка голосовых каналов

Бот может подключаться и удерживать присутствие в голосовых каналах:

```json
"voice_chat": {
  "enabled": true,
  "server_id": "1234567890123456789",
  "channel_id": "9876543210987654321",
  "user_id": "1111222233334444555"
}
```

**Возможности:**

* Автоматическое подключение к голосовым каналам
* Механизм heartbeat для удержания соединения
* Автовосстановление соединения при разрыве (до 5 попыток с экспоненциальным бэкоффом)
* Поддержка WebRTC
* Корректное отключение

**Требования:**

* Права на подключение в голосовой канал
* Токен должен иметь доступ к серверу
* User ID должен соответствовать токену

---

### ⚠️ Важные замечания

* ✅ Убедитесь, что токены действительны и имеют нужные разрешения
* 🌐 Для снижения риска блокировок используйте прокси (рекомендуется 1 прокси на токен)
* ⏱️ Настраивайте задержки аккуратно для большого количества аккаунтов
* 📊 Следите за логами и при необходимости корректируйте `servers.json`
* 🔒 Храните токены и API-ключи в безопасности
* 🚫 Не делитесь `tokens.txt` с кем-либо
* 💾 Бот автоматически сохраняет ошибки в `error-channels.json`
* 🔄 Для задач отправки ссылок используйте submit-only режим
* 🎯 Выставляйте адекватные cooldown’ы, чтобы избежать спам-детекции
* 🤖 AI-провайдеры пробуются по очереди до успешного результата
* 📸 Если Freepik не отвечает — используется бесплатный Pollinations.ai
* ⚡ Флаги CLI переопределяют все другие настройки

</details>

---

## 📞 Контакты и поддержка

* 🛒 **Поддержать автора:** [Ссылка 1](https://t.me/KeoAirDropFreeNe/312/27801) • [Ссылка 2](https://github.com/MeoMunDep/MeoMunDep)
* 💬 **Контакт:** [@MeoMunDep](https://t.me/MeoMunDep)
* 👥 **Группа:** [Присоединиться](https://t.me/KeoAirDropFreeNe)
* 📢 **Канал:** [Перейти](https://t.me/KeoAirDropFreeNee)

---

⚠️ **Дисклеймер**

Этот проект создан исключительно в качестве **личного образовательного упражнения**.
Он **не является официальным продуктом**, сайтом или сервисом каких-либо упомянутых компаний или брендов.
Автор **не связан, не аффилирован и не поддерживается** никакими из упомянутых брендов.

Код предоставляется **"как есть"** без гарантий и обязательств.
Некоторые части исходников намеренно **обфусцированы** для защиты личных исследований, логики и технических решений.

Используйте этот проект ответственно и на свой риск.
**Перераспространение, перепродажа или коммерческое использование** любой части кода — запрещены.

---

<p align="center"><a href="#-discord-automation-bot-от-meomundep">⬆️ Наверх</a></p>

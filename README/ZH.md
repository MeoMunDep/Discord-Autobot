# 🤖 Discord 自动化机器人 — 作者 @MeoMunDep

<p align="center">
  <img src="https://github.com/MeoMunDep/Discord-Autobot/blob/main/IMAGE/DISCORD_MEOMUNDEP_CLI.png?raw=true" width="600" />
</p>

---

## 🌐 概览

该机器人自动化在 **Discord** 上的交互，包括发送消息、加入/离开服务器，以及使用 **多个账户** 并支持 **代理** 的消息处理。

---

## 🚀 功能

* 💬 **自动发送消息** — 向指定频道发送消息并可自定义延迟
* 🔗 **提交频道管理** — 专门用于 X/Twitter 帖子提交的系统
* ➕ **自动加入服务器** — 使用邀请链接加入服务器
* ➖ **自动离开服务器** — 离开指定的服务器
* 🌍 **代理支持** — 支持 HTTP(S) 与 SOCKS5 代理
* 🗑️ **自动删除消息** — 发送后可在配置的延迟后删除消息
* 🌐 **多语言支持** — 可配置消息语言
* 📕 **错误日志** — 保存发生错误的服务器或频道 ID
* 🤖 **AI 回复** — 使用 AI 提供者生成随意回复（支持 Groq、OpenRouter、Gemini、Poe、Grok）
* 🖼️ **图片生成** — 使用 Freepik API（失败回退到 Pollinations.ai）在频道生成并发送 AI 图片
* 🎛️ **服务器控制** — 可开启/关闭单个服务器而无需删除配置
* ⚙️ **双模式运行** — 常规模式（完整流程）或仅提交模式（submit-only）
* 🎮 **语音频道支持** — 加入并维持在语音频道中的存在
* 📊 **交互式管理** — 内置 CLI 菜单用于服务器、延迟与错误管理
* 🚀 **命令行参数覆盖** — 使用 CLI 参数覆盖配置设置

---

<details>
<summary><strong>🛠️ 安装</strong></summary>

### 📋 要求

* **Node.js** `v22.11.0`
* **npm** `v10.9.0`

📦 下载：[Node.js & npm](https://t.me/KeoAirDropFreeNe/257/1462)

---

### 📥 安装步骤

1. **克隆仓库：**

   ```bash
   git clone https://github.com/MeoMunDep/Discord-Autobot.git
   cd "discord-autobot"
   ```

2. **安装依赖：**

   ```bash
   npm install
   ```

   ⚠️ 若在 Windows 上遇到 *Execution Policy* 错误：

   ```bash
   Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
   ```

   然后重新运行：

   ```bash
   npm install
   ```

3. **配置机器人：** 编辑 `configs.json` 以符合你的偏好。

4. **准备输入文件：**

   * 📄 `tokens.txt` — Discord 令牌（每行一个）
   * 🆔 `servers.json` — 包含服务器详情的文件（guild IDs、invite codes、频道配置 — 详见下文）
   * 🌐 `proxies.txt` — 代理（可选，每个 token 一行）
   * 📜 `messages.yaml` — 多语言消息
   * 🗂️ `error-channels.json` — 存储失败的频道/服务器（自动生成/更新）

📌 有用链接：

* 🔑 获取 tokens: [Here](https://t.me/KeoAirDropFreeNee/1570)
* 💬 获取 chat ID: [Here](https://t.me/KeoAirDropFreeNee/1569)
* 📡 获取 channel/server ID: [Here](https://t.me/KeoAirDropFreeNee/1676)

</details>

---

<details>
<summary><strong>⚙️ 配置（configs.json）</strong></summary>

| ⚙️ 设置                          | 📘 说明                                                      |
| ------------------------------ | ---------------------------------------------------------- |
| `max_accounts_at_once`         | 同时运行的账户数量                                                  |
| `delay_between_accounts`       | 每个账户启动之间的延迟（秒）                                             |
| `delay_each_channel`           | 不同频道之间的延迟范围（秒） `[min, max]`                                |
| `delay_each_chat`              | 同一频道内消息之间的延迟范围（秒） `[min, max]`                             |
| `time_to_restart_all_accounts` | 重启所有账户前的时间（秒）                                              |
| `chat_language`                | 自动生成消息所用语言                                                 |
| `ask_before_auto_chat`         | 在每个循环前提示用户选择模式（submit_only / normal）                       |
| `auto_chat`                    | 默认模式：`true` = normal 模式, `false` = submit_only 模式          |
| `submit_x_url`                 | 是否在常规频道提交 X/Twitter 链接                                     |
| `join_server`                  | 是否启用自动加入服务器                                                |
| `leave_server`                 | 是否启用自动离开服务器                                                |
| `delete_message`               | 是否在发送后删除消息                                                 |
| `enable_replies`               | 是否启用消息回复                                                   |
| `enable_reactions`             | 是否启用对消息的表情反应                                               |
| `reaction_chance`              | 反应的概率（0–1）                                                 |
| `enable_ai_replies`            | 是否启用 AI 回复                                                 |
| `ai_reply_chance`              | 使用 AI 回复的概率（0–1）                                           |
| `ai_providers.*`               | AI 提供者的 API keys（数组） (Groq, Gemini, Poe, OpenRouter, Grok) |
| `image_generation_providers.*` | 图像 AI 提供者的 API keys（数组）（Freepik）                           |
| `voice_chat.enabled`           | 是否启用语音频道连接                                                 |
| `voice_chat.server_id`         | 要加入的语音频道所在 Guild ID                                        |
| `voice_chat.channel_id`        | 语音频道 ID                                                    |
| `voice_chat.user_id`           | 用于语音连接的用户 ID                                               |

---

### 🧾 示例 `configs.json`

```json
{
  "max_accounts_at_once": 1,
  "delay_between_accounts": 15,
  "delay_each_channel": [5, 7],
  "delay_each_chat": [10, 15],
  "time_to_restart_all_accounts": 963,

  "ask_before_auto_chat": false,
  "auto_chat": true,
  "submit_x_url": false,
  "delete_message": false,
  
  "join_server": false,
  "leave_server": false,

  "enable_replies": true,
  "ai_reply_chance": 0.7,

  "enable_reactions": true,
  "reaction_chance": 0.7,
  
  "enable_ai_replies": true,
  "ai_providers": {
    "groq": ["gsk_xxxxxxxxxxxxxxxxxxxxx"],
    "openrouter": [
      "sk-or-v1-xxxxxxxxxxxxx",
      "sk-or-v1-yyyyyyyyyyyyy"
    ],
    "gemini": [
      "AIzaSyXXXXXXXXXXXXXXX",
      "AIzaSyYYYYYYYYYYYYYYY",
      "AIzaSyZZZZZZZZZZZZZZZ"
    ],
    "poe": [""],
    "grok": ["xai-xxxxxxxxxxxxxxxxxxxxx"]
  },

  "image_generation_providers": {
    "freepik": [
      "fpk_xxxxxxxxxxxxxxxxxxxxx",
      "fpk_yyyyyyyyyyyyyyyyyyyyy"
    ]
  },

  "voice_chat": {
    "enabled": false,
    "server_id": "",
    "channel_id": "",
    "user_id": ""
  }
}
```

---

### 🔑 多个 API Key

机器人支持为每个提供者配置 **多个 API key**。它会在每次请求时随机选择数组中的一个 key：

```json
"ai_providers": {
  "groq": ["key1", "key2", "key3"],
  "gemini": ["key1", "key2"]
}
```

这有助于：

* **负载均衡**（跨多个 API 账号分配请求）
* **速率限制管理**（通过分散请求降低单 key 的限额压力）
* **可靠性**（若某个 key 失败，可尝试其它 key）

---

<details>
<summary><strong>🔑 GROQ API Key</strong></summary>

[创建 Key](https://console.groq.com/keys)

<p align="center">
  <img src="https://github.com/MeoMunDep/Discord-Autobot/blob/main/IMAGE/GROQ_API_KEY.png?raw=true" width="600" />
</p>

</details>

<details>
<summary><strong>🔑 GROK API Key</strong></summary>

[创建 Key](https://console.x.ai/api-keys)

**备注：** Grok API 使用与 OpenAI 兼容的格式。请在 X.AI 控制台获取 API key。

</details>

<details>
<summary><strong>🔑 GEMINI API Key</strong></summary>

[创建 Key](https://aistudio.google.com/app/apikey)

<p align="center">
  <img src="https://github.com/MeoMunDep/Discord-Autobot/blob/main/IMAGE/GEMINI_API_KEY.png?raw=true" width="600" />
</p>

</details>

<details>
<summary><strong>🔑 OPENROUTER API Key</strong></summary>

[创建 Key](https://openrouter.ai/settings/keys)

<p align="center">
  <img src="https://github.com/MeoMunDep/Discord-Autobot/blob/main/IMAGE/OPENROUTER_API_KEY.png?raw=true" width="600" />
</p>

</details>

<details>
<summary><strong>🔑 POE API Key</strong></summary>

[创建 Key](https://poe.com/api_key)

<p align="center">
  <img src="https://github.com/MeoMunDep/Discord-Autobot/blob/main/IMAGE/POE_API_KEY.png?raw=true" width="600" />
</p>

</details>

<details>
<summary><strong>🔑 FREEPIK API Key</strong></summary>

[创建 Key](https://api.freepik.com/v1/auth/create)

<p align="center">
  <img src="https://github.com/MeoMunDep/Discord-Autobot/blob/main/IMAGE/FREEPIK_API_KEY.png?raw=true" width="600" />
</p>

</details>

</details>

---

<details>
<summary><strong>🗂️ servers.json 结构（详细指南）</strong></summary>

`servers.json` 文件是机器人配置的 **核心**，定义了机器人要在哪些服务器上进行哪些动作以及在每个频道中执行什么操作。

---

## 📚 基本结构

该文件应为一个 **JSON 数组**，包含多个服务器对象。每个服务器对象表示机器人应与之交互的 Discord 服务器。

```json
[
  {
    "name": "Server Name",
    "guild_id": "1234567890123456789",
    "invite_id": "inviteCode",
    "enabled": true,
    "channels": {
      "submit_chat": { ... },
      "raw_chat": { ... },
      "command_chat": { ... },
      "image_chat": { ... }
    }
  }
]
```

---

## 🔑 字段说明

### 1️⃣ `name`（字符串 - 必需）

* **说明：** 服务器的友好名称（仅供参考）
* **示例：** `"Curious"`, `"My Gaming Server"`, `"Crypto Community"`
* **用途：** 在日志和配置中帮助识别服务器

### 2️⃣ `guild_id`（字符串 - 必需）

* **说明：** Discord 服务器的唯一 ID
* **获取方法：** 参见指南（链接上文）
* **示例：** `"1417067849926705235"`
* **用途：** 在 `leave_server: true` 时用于离开服务器

### 3️⃣ `invite_id`（字符串 - 必需）

* **说明：** 加入服务器的邀请 code
* **示例：** `"altiuslabs"`（对应 discord.gg/altiuslabs）
* **特殊值：**

  * `""`（空字符串）- 不加入该服务器
  * `"none"` - 不加入该服务器
  * `"unknown"` - 不加入该服务器
* **用途：** 在 `join_server: true` 时用于加入服务器

### 4️⃣ `enabled`（布尔 - 必需）

* **说明：** 控制机器人是否处理该服务器
* **值：**

  * `true` - 处理该服务器（发送消息、加入等）
  * `false` - 跳过该服务器
* **用途：** 临时禁用服务器而不删除配置

### 5️⃣ `channels`（对象 - 必需）

定义机器人在每个频道要执行的操作。包含四种频道类型：

---

## 🗨️ 频道类型

### 🔗 `submit_chat` — X/Twitter 帖子提交（新）

**目的：** 专门用于提交 X/Twitter 帖子链接，可自定义延迟。

**格式：**

```json
"submit_chat": {
  "CHANNEL_ID": {
    "message": "https://x.com/yourpost",
    "delay": [6000, 12000]
  }
}
```

**字段：**

* `message`（字符串）：要提交的 X/Twitter URL
* `delay`（数组）：频道再次发布前的冷却时间（秒）[min, max]
* `enabled`（布尔，可选）：将该频道单独禁用而不删除配置

**示例：**

```json
"submit_chat": {
  "1380138116505141288": {
    "message": "https://x.com/MeoMunDep/status/123456789",
    "delay": [6000, 12000]
  },
  "1441334930268225637": {
    "message": "https://x.com/CryptoProject/status/987654321",
    "delay": [3600, 7200],
    "enabled": false
  }
}
```

**工作方式：**

* 机器人仅在 **submit-only** 模式或正常模式下处理这些频道
* 每个频道有独立的冷却计时器
* URL 将按照提供的文本原样提交
* 若 `configs.json` 中 `submit_x_url: false`，常规频道中的 X/Twitter URL 将被跳过

---

### 📝 `raw_chat` — 文本消息

用于发送常规文本消息。

**简单格式：**

```json
"raw_chat": {
  "CHANNEL_ID": "MESSAGE_TO_SEND"
}
```

**进阶格式（带选项）：**

```json
"raw_chat": {
  "CHANNEL_ID": {
    "message": "MESSAGE_TO_SEND",
    "delay": [60, 120],
    "enabled": true
  }
}
```

**特殊值：**

* `"__random_message"` — 机器人将使用 AI 或 `messages.yaml` 中的随机消息
* `"[AI] Your instruction"` — 机器人将使用 AI 按照你的指令生成消息
* 任何具体文本 — 机器人按该文本发送（例如 `"gm"`、`"Hello!"`）

**示例：**

```json
"raw_chat": {
  "1417068171243815002": {
    "message": "[AI] Respond to random messages",
    "delay": [60, 120]
  },
  "1417084328504197250": {
    "message": "gm",
    "delay": [6000, 12000]
  },
  "9876543210987654321": "__random_message"
}
```

**行为说明：**

* 在频道 `1417068171243815002`：机器人会按指令使用 AI 生成回复
* 在频道 `1417084328504197250`：机器人每 6000–12000 秒发送一次 `"gm"`
* 在频道 `9876543210987654321`：机器人发送随机 AI 生成的消息

---

### 🤖 `command_chat` — 发送命令

在频道中发送斜线命令或普通机器人命令。

**格式：**

```json
"command_chat": {
  "CHANNEL_ID": {
    "command": "COMMAND_TO_EXECUTE",
    "delay": [60, 120]
  }
}
```

**示例：**

```json
"command_chat": {
  "1234567890123456789": {
    "command": "/claim daily",
    "delay": [86400, 86500]
  },
  "9876543210987654321": {
    "command": "!rank",
    "delay": [600, 1200]
  }
}
```

---

### 🖼️ `image_chat` — AI 生成图片

使用 Freepik API（失败则回退到 Pollinations.ai）生成并发送图片到频道。

**格式：**

```json
"image_chat": {
  "CHANNEL_ID": {
    "prompt": "IMAGE_GENERATION_PROMPT",
    "delay": [60, 120]
  }
}
```

**示例：**

```json
"image_chat": {
  "1435601397591703675": {
    "prompt": "Airdrop project with 'Curious'",
    "delay": [7200, 10000]
  },
  "9876543210987654321": {
    "prompt": "Beautiful sunset over mountains",
    "delay": [3600, 7200]
  }
}
```

**要求：**

* 必须在 `configs.json` 中配置 `freepik` API key
* 若 Freepik 失败或未配置 API key，机器人自动使用 Pollinations.ai（免费，无需 API key）

---

## 📋 完整示例

```json
[
  {
    "name": "Curious",
    "guild_id": "1417067849926705235",
    "invite_id": "curious",
    "enabled": true,
    "channels": {
      "submit_chat": {
        "1417080821852147732": {
          "message": "https://x.com/Curious/status/123456",
          "delay": [6000, 12000]
        }
      },
      "raw_chat": {
        "1417068171243815002": {
          "message": "[AI] Respond to random messages",
          "delay": [60, 120]
        },
        "1417084328504197250": {
          "message": "gcurious",
          "delay": [600, 1200]
        }
      },
      "command_chat": {
        "1417068171243815003": {
          "command": "!rank",
          "delay": [600, 1200]
        }
      },
      "image_chat": {
        "1435601397591703675": {
          "prompt": "Airdrop project with 'CURIOUS'",
          "delay": [60, 120]
        }
      }
    }
  },
  {
    "name": "Disabled Server (Not Active)",
    "guild_id": "5555555555555555555",
    "invite_id": "oldserver",
    "enabled": false,
    "channels": {
      "submit_chat": {},
      "raw_chat": {
        "3333333333333333333": {
          "message": "test message",
          "delay": [60, 120]
        }
      },
      "command_chat": {},
      "image_chat": {}
    }
  }
]
```

---

## ⏱️ 延迟系统

机器人使用 **智能冷却系统** 以避免垃圾信息与速率限制：

### 📊 延迟优先级（从高到低）：

1. **频道专属延迟**（在 `servers.json` 中）

   ```json
   "1234567890": {
     "message": "gm",
     "delay": [300, 600]  ← 优先使用此延迟
   }
   ```

2. **全局 delay_each_chat**（在 `configs.json` 中）

   ```json
   "delay_each_chat": [10, 15]  ← 若频道未设置延迟则使用此值
   ```

### 🔄 工作原理：

* 每个频道有独立的 **冷却计时器**
* 在成功发送消息后开始计时
* 在冷却期内，机器人会跳过该频道并记录剩余等待时间
* 冷却时间是 **持久化** 的，保存在 `chat-timestamps.json` 中
* 机器人在动作之间遵循配置中的延迟与重启间隔

### 💡 建议：

* 对低活跃频道使用更长延迟（3600–7200 秒）
* 对高参与频道使用较短延迟（60–300 秒）
* 提交频道通常使用 6000–12000 秒的延迟
* 图片生成建议使用更长的延迟（7200–10000 秒）以控制 API 成本

---

## 💡 使用技巧与最佳实践

### ✅ 推荐做法：

* 使用描述性服务器名称便于识别
* 临时禁用服务器用 `enabled: false`，避免删除配置
* 单独禁用频道用其 `enabled: false`
* 对频道设置专属延迟以获得更好控制
* 对自定义 AI 回复使用 `"[AI] instruction"` 格式
* 使用 `submit_chat` 专门提交 X/Twitter 链接而不是常规频道
* 先在单服务器上测试，再扩展到多个服务器
* 对图片生成使用更长延迟以节省 API 成本

### ❌ 不建议做法：

* 不要删除服务器条目 — 使用 `enabled: false`
* 不要忘记为频道添加延迟（虽然有默认值，但显式配置更安全）
* 避免短于 60 秒的延迟以减少被限流或封禁风险
* 在 JSON 中确保语法正确（使用 JSON 验证工具）

---

## 🔍 查找频道 ID

1. 在 Discord 中启用开发者模式（设置 → 高级 → 开发者模式）
2. 右键点击任意频道
3. 点击 “复制频道 ID”
4. 将 ID 粘贴到 `servers.json`

[视觉指南](https://t.me/KeoAirDropFreeNee/1676)

---

## 🐛 故障排查

| 问题        | 解决办法                                                               |
| --------- | ------------------------------------------------------------------ |
| 机器人跳过服务器  | 检查 `enabled: true` 是否设置                                            |
| 机器人跳过特定频道 | 检查频道配置是否包含 `enabled: false`                                        |
| 无法加入服务器   | 检查 `invite_id` 是否正确，且 `join_server: true` 在 configs 中启用            |
| 无法发送消息    | 检查频道 ID 是否正确，且 `auto_chat: true` 在 configs 中启用                     |
| 消息发送频率过高  | 为频道添加或增加 `delay` 值                                                 |
| 图片无法生成    | 检查 Freepik API key 是否在 `configs.json` 中配置，或机器人会回退到 Pollinations.ai |
| 提交频道不工作   | 确认以正确的模式运行（submit-only 或 normal）                                   |
| JSON 语法错误 | 使用 JSON 验证工具（例如 jsonlint.com）                                      |

</details>

---

<details>
<summary><strong>▶️ 使用说明</strong></summary>

### 📌 运行机器人

**基本运行：**

```bash
node index_meomundep.js
```

**使用命令行参数：**

```bash
# 强制 submit-only 模式（仅处理 submit_chat 频道）
node index_meomundep.js --submit
node index_meomundep.js -s

# 强制 normal 模式（完整流程：加入、聊天、离开）
node index_meomundep.js --chat
node index_meomundep.js -c

# 显示帮助
node index_meomundep.js --help
node index_meomundep.js -h
```

### 🎮 运行模式

机器人支持 **两种运行模式**：

#### 1️⃣ 正常模式（完整流程）

* 如果 `join_server: true` 则加入服务器
* 处理所有频道类型：`submit_chat`, `raw_chat`, `command_chat`, `image_chat`
* 如果 `leave_server: true` 则离开服务器
* 使用 AI 生成消息

**激活方式：**

* CLI: `node index_meomundep.js --chat`
* 配置: `"auto_chat": true`（默认）
* 运行时: 若 `ask_before_auto_chat: true`，选择选项 `2`

#### 2️⃣ 仅提交模式（Submit-Only）

* 跳过加入/离开服务器
* **仅** 处理 `submit_chat` 频道
* 忽略 `raw_chat`, `command_chat`, `image_chat`
* 适用于专门的 X/Twitter 提交任务

**激活方式：**

* CLI: `node index_meomundep.js --submit`
* 配置: `"auto_chat": false`
* 运行时: 若 `ask_before_auto_chat: true`，选择选项 `1`

### 🔄 模式优先级（从高到低）：

1. **CLI 参数** (`--submit` 或 `--chat`)
2. **运行时提示**（如果 `ask_before_auto_chat: true`）
3. **配置设置**（`auto_chat` 的值）

### 📊 交互式管理

机器人内置管理系统，运行后可通过主菜单访问：

```bash
node index_meomundep.js
# 然后从主菜单选择 "Server Manager"
```

**管理功能：**

🖥️ **主菜单：**

1. 服务器管理（启用/禁用服务器）
2. 延迟管理（添加/显示延迟）
3. 提交管理（X 帖子链接）
4. 错误频道管理
5. 退出

📡 **服务器管理：**

* 一次启用/禁用所有服务器
* 启用/禁用特定服务器
* 仅启用一个服务器（禁用其他所有）
* 显示服务器状态概览

⏱️ **延迟管理：**

* 为缺少延迟的频道添加默认延迟
* 添加自定义延迟
* 显示延迟统计
* 列出正在使用的独特延迟值

🔗 **提交管理：**

* 列出所有 submit 频道
* 添加新的 submit 频道
* 编辑 submit URL
* 删除 submit 频道
* 显示提交统计

🚫 **错误频道管理：**

* 按 token 查看所有错误频道
* 删除特定错误频道
* 删除整个 token 条目
* 清理失败频道记录

### ⬆️ 更新机器人

```bash
git pull
npm install
```

---

### 🔍 工作流程概览

1. 从 `configs.json` 读取配置
2. 检查 CLI 参数（`--submit`, `--chat`, `--help`）
3. 若 `ask_before_auto_chat: true` 且无 CLI 参数，则提示用户
4. 确定运行模式（submit-only 或 normal）
5. 使用 `tokens.txt` 中的 token 登录
6. 若 `proxies.txt` 提供代理则为每个账户应用代理
7. 处理 `servers.json` 中的每个服务器：

   * ✅ 检查 `enabled: true`
   * ✅ 检查频道是否未被 `enabled: false` 禁用
   * ✅ 检查冷却计时器后再发送消息
   * **在正常模式下：**

     * ➕ 若 `join_server: true` 则加入服务器（使用 `invite_id`）
     * 💬 处理所有频道类型
     * ➖ 若 `leave_server: true` 则离开服务器（使用 `guild_id`）
   * **在 submit-only 模式下：**

     * 🔗 仅处理 `submit_chat` 频道
     * ⏭️ 跳过加入/离开与其他频道类型
8. 记录每个事件并在控制台显示带颜色的日志
9. 保存时间戳以避免在冷却期内重复发布
10. 在动作间等待配置的延迟
11. 在 `time_to_restart_all_accounts` 秒后自动重复运行

---

### 📝 日志与时间戳

* 每条日志包含时间戳
* 时间戳格式遵循 `chat_language` 设置
* 控制台使用颜色区分日志类型：

  * 🟢 绿色 - 成功
  * 🔴 红色 - 错误
  * 🟡 黄色 - 警告
  * 🔵 蓝色 - 信息
  * 🟣 紫色 - 处理中
  * 🔶 橙色 - AI/提供者调用

---

### 🎙️ 语音频道支持

机器人可加入并维持在语音频道中的连接：

```json
"voice_chat": {
  "enabled": true,
  "server_id": "1234567890123456789",
  "channel_id": "9876543210987654321",
  "user_id": "1111222233334444555"
}
```

**功能：**

* 自动连接到语音频道
* 心跳机制以维持连接
* 断开后自动重连（最多 5 次，使用指数退避）
* 使用 WebRTC 协议
* 干净的断开处理

**要求：**

* 需要有权限加入语音频道
* token 必须能访问目标服务器
* `user_id` 必须与 token 对应的用户 ID 匹配

---

### ⚠️ 注意事项

* ✅ 确保所有 token 有效并拥有必要权限
* 🌐 使用代理以降低被限流或封禁的风险（建议每个 token 对应一个代理）
* ⏱️ 为大量账户调整延迟设置以避免速率限制
* 📊 监视日志以发现错误并调整 `servers.json` 配置
* 🔒 妥善保管 tokens 与 API keys，勿在公共场合泄露
* 🚫 切勿与他人共享 `tokens.txt` 文件
* 💾 机器人会自动保存错误到 `error-channels.json` 以避免重复尝试失败的频道
* 🔄 对于提交任务，使用 submit-only 模式更稳妥
* 🎯 为避免滥用，请设置合适的冷却时间与发送频率
* 🤖 AI 提供者会按顺序尝试，直到某个提供者成功返回结果
* 📸 若 Freepik 失败，将自动回退到免费的 Pollinations.ai

</details>

---

## 📞 联系与支持

* 🛒 **支持我：** [Link 1](https://t.me/KeoAirDropFreeNe/312/27801) • [Link 2](https://github.com/MeoMunDep/MeoMunDep)
* 💬 **联系：** [@MeoMunDep](https://t.me/MeoMunDep)
* 👥 **群组：** [加入群组](https://t.me/KeoAirDropFreeNe)
* 📢 **频道：** [访问频道](https://t.me/KeoAirDropFreeNee)

---

⚠️ **免责声明**

该项目仅作为 **个人学习练习** 创建。
它 **并非任何被引用公司或品牌的官方产品、网站或服务**。
创建者 **不与任何该等公司或品牌关联、背书或连接**。

此代码按 **“原样”** 提供，不作任何保证。
源码的某些部分可能会被故意混淆以保护个人研究、定制逻辑和学习期间开发的实现技巧。

请负责且谨慎地使用本项目。
**禁止转售、再分发或商业用途**（无论原始或修改后）。

---

<p align="center"><a href="#-discord-自动化机器人-—-作者-meomundep">⬆️ 返回顶部</a></p>

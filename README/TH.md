# 🤖 บอทอัตโนมัติสำหรับ Discord โดย @MeoMunDep

<p align="center">
  <img src="https://github.com/MeoMunDep/Discord-Autobot/blob/main/IMAGE/DISCORD_MEOMUNDEP_CLI.png?raw=true" width="600" />
</p>

---

## 🌐 ภาพรวม

บอทนี้ทำการอัตโนมัติการโต้ตอบบน **Discord** รวมถึงการส่งข้อความ การเข้าร่วม/ออกจากเซิร์ฟเวอร์ และจัดการข้อความโดยใช้ **หลายบัญชี** พร้อมรองรับ **พร็อกซี่**.

---

## 🚀 ฟีเจอร์

* 💬 **ส่งข้อความอัตโนมัติ** — ส่งข้อความไปยังช่องที่กำหนดพร้อมหน่วงเวลาที่ปรับได้
* 🔗 **ระบบจัดการการส่งโพสต์ (Submit Channel)** — ระบบสำหรับส่งโพสต์ X/Twitter โดยเฉพาะ
* ➕ **เข้าร่วมเซิร์ฟเวอร์อัตโนมัติ** — เข้าร่วมเซิร์ฟเวอร์ด้วยลิงก์เชิญ
* ➖ **ออกจากเซิร์ฟเวอร์อัตโนมัติ** — ออกจากเซิร์ฟเวอร์ที่กำหนด
* 🌍 **รองรับพร็อกซี่** — รองรับ HTTP(S) และ SOCKS5
* 🗑️ **ลบข้อความอัตโนมัติ** — ลบข้อความที่ส่งหลังจากหน่วงเวลาที่กำหนด
* 🌐 **รองรับหลายภาษา** — กำหนดภาษาของข้อความอัตโนมัติได้
* 📕 **บันทึกข้อผิดพลาด** — บันทึก ID ของเซิร์ฟเวอร์หรือช่องที่เกิดข้อผิดพลาด
* 🤖 **ตอบกลับด้วย AI** — สร้างการตอบแบบไม่เป็นทางการโดยใช้ผู้ให้บริการ AI (Groq, OpenRouter, Gemini, Poe, Grok)
* 🖼️ **สร้างภาพด้วย AI** — สร้างและส่งภาพ AI ไปยังช่อง โดยใช้ Freepik API (สำรองด้วย Pollinations.ai)
* 🎛️ **ควบคุมเซิร์ฟเวอร์** — เปิด/ปิดการทำงานของเซิร์ฟเวอร์แต่ละตัวโดยไม่ต้องลบการตั้งค่า
* ⚙️ **โหมดคู่** — รันได้ทั้งโหมดปกติ (ไหลงานเต็ม) หรือโหมดส่งเฉพาะ (submit-only)
* 🎮 **รองรับช่องเสียง (Voice)** — เข้าร่วมและรักษาการเชื่อมต่อในช่องเสียง
* 📊 **จัดการแบบโต้ตอบ** — เมนู CLI ในตัวสำหรับจัดการเซิร์ฟเวอร์ หน่วงเวลา และข้อผิดพลาด
* 🚀 **แฟล็ก CLI** — เข้ากำหนดค่าทับการตั้งค่าในไฟล์ด้วยอาร์กิวเมนต์บรรทัดคำสั่ง

---

<details>
<summary><strong>🛠️ การติดตั้ง</strong></summary>

### 📋 ข้อกำหนด

* **Node.js** `v22.11.0`
* **npm** `v10.9.0`

📦 ดาวน์โหลด: [Node.js & npm](https://t.me/KeoAirDropFreeNe/257/1462)

---

### 📥 ขั้นตอน

1. **โคลนรีโพ:**

   ```bash
   git clone https://github.com/MeoMunDep/Discord-Autobot.git
   cd "discord-autobot"
   ```

2. **ติดตั้ง dependencies:**

   ```bash
   npm install
   ```

   ⚠️ หากเจอข้อผิดพลาด *Execution Policy* บน Windows:

   ```bash
   Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
   ```

   แล้วรันอีกครั้ง:

   ```bash
   npm install
   ```

3. **ปรับค่า bot:**
   แก้ไขไฟล์ `configs.json` ให้ตรงกับความต้องการของคุณ

4. **เตรียมไฟล์อินพุต:**

   * 📄 `tokens.txt` — โทเค็น Discord (บรรทัดละโทเค็น)
   * 🆔 `servers.json` — รายละเอียดเซิร์ฟเวอร์ รวม guild IDs, invite codes, และการตั้งค่าช่อง (ดูคำอธิบายด้านล่าง)
   * 🌐 `proxies.txt` — พร็อกซี่ (ตัวเลือก, บรรทัดละพร็อกซี่ สำหรับแต่ละโทเค็น)
   * 📜 `messages.yaml` — ข้อความ (หลายภาษา)
   * 🗂️ `error-channels.json` — เก็บช่อง/เซิร์ฟเวอร์ที่ล้มเหลว (สร้าง/อัปเดตโดยอัตโนมัติ)

📌 ลิงก์ช่วยเหลือ:

* 🔑 ขอโทเค็น: [Here](https://t.me/KeoAirDropFreeNee/1570)
* 💬 รับ chat ID: [Here](https://t.me/KeoAirDropFreeNee/1569)
* 📡 รับ channel/server ID: [Here](https://t.me/KeoAirDropFreeNee/1676)

</details>

---

<details>
<summary><strong>⚙️ การกำหนดค่า (configs.json)</strong></summary>

| ⚙️ การตั้งค่า                  | 📘 คำอธิบาย                                                                   |
| ------------------------------ | ----------------------------------------------------------------------------- |
| `max_accounts_at_once`         | จำนวนบัญชีที่รันพร้อมกัน                                                      |
| `delay_between_accounts`       | หน่วงเวลา (วินาที) ระหว่างการสตาร์ทแต่ละบัญชี                                 |
| `delay_each_channel`           | ช่วงหน่วงเวลา [min, max] (วินาที) ระหว่างช่องต่าง ๆ                           |
| `delay_each_chat`              | ช่วงหน่วงเวลา [min, max] (วินาที) ระหว่างข้อความในช่องเดียวกัน                |
| `time_to_restart_all_accounts` | เวลา (วินาที) ก่อนรีสตาร์ททุกบัญชี                                            |
| `chat_language`                | ภาษาที่ใช้สำหรับข้อความอัตโนมัติ                                              |
| `ask_before_auto_chat`         | ถามผู้ใช้ก่อนเลือกระหว่างโหมด (submit_only/normal) ก่อนรอบแต่ละครั้ง          |
| `auto_chat`                    | โหมดเริ่มต้น: true = โหมดปกติ, false = โหมด submit-only                       |
| `submit_x_url`                 | เปิด/ปิด การส่ง URL ของ X/Twitter ในช่องปกติ                                  |
| `join_server`                  | เปิด/ปิด การเข้าร่วมเซิร์ฟเวอร์อัตโนมัติ                                      |
| `leave_server`                 | เปิด/ปิด การออกจากเซิร์ฟเวอร์อัตโนมัติ                                        |
| `delete_message`               | เปิด/ปิด การลบข้อความหลังส่ง                                                  |
| `enable_replies`               | เปิด/ปิด การตอบกลับข้อความ                                                    |
| `enable_reactions`             | เปิด/ปิด การทำ reaction กับข้อความ                                            |
| `reaction_chance`              | ความน่าจะเป็น (0–1) ที่จะทำ reaction                                          |
| `enable_ai_replies`            | เปิด/ปิด การตอบกลับด้วย AI                                                    |
| `ai_reply_chance`              | ความน่าจะเป็น (0–1) ที่จะใช้ AI ในการตอบ                                      |
| `ai_providers.*`               | API keys (arrays) สำหรับผู้ให้บริการ AI (Groq, Gemini, Poe, OpenRouter, Grok) |
| `image_generation_providers.*` | API keys (arrays) สำหรับผู้ให้บริการภาพ (Freepik)                             |
| `voice_chat.enabled`           | เปิด/ปิด การเชื่อมต่อช่องเสียง                                                |
| `voice_chat.server_id`         | Guild ID สำหรับช่องเสียง                                                      |
| `voice_chat.channel_id`        | Channel ID ของช่องเสียงที่จะเข้าร่วม                                          |
| `voice_chat.user_id`           | User ID ของคุณสำหรับการเชื่อมต่อเสียง                                         |

---

### 🧾 ตัวอย่าง `configs.json`

```json
{
  "max_accounts_at_once": 1,
  "delay_between_accounts": 15,
  "delay_each_channel": [5, 7],
  "delay_each_chat": [10, 15],
  "time_to_restart_all_accounts": 963,

  "ask_before_auto_chat": false,
  "auto_chat": true,
  "submit_x_url": false,
  "delete_message": false,
  
  "join_server": false,
  "leave_server": false,

  "enable_replies": true,
  "ai_reply_chance": 0.7,

  "enable_reactions": true,
  "reaction_chance": 0.7,
  
  "enable_ai_replies": true,
  "ai_providers": {
    "groq": ["gsk_xxxxxxxxxxxxxxxxxxxxx"],
    "openrouter": [
      "sk-or-v1-xxxxxxxxxxxxx",
      "sk-or-v1-yyyyyyyyyyyyy"
    ],
    "gemini": [
      "AIzaSyXXXXXXXXXXXXXXX",
      "AIzaSyYYYYYYYYYYYYYYY",
      "AIzaSyZZZZZZZZZZZZZZZ"
    ],
    "poe": [""],
    "grok": ["xai-xxxxxxxxxxxxxxxxxxxxx"]
  },

  "image_generation_providers": {
    "freepik": [
      "fpk_xxxxxxxxxxxxxxxxxxxxx",
      "fpk_yyyyyyyyyyyyyyyyyyyyy"
    ]
  },

  "voice_chat": {
    "enabled": false,
    "server_id": "",
    "channel_id": "",
    "user_id": ""
  }
}
```

---

### 🗝️ การใช้หลาย API Keys

บอทรองรับ **หลาย API keys** สำหรับแต่ละผู้ให้บริการ จะสุ่มเลือกหนึ่งคีย์จากอาร์เรย์สำหรับแต่ละคำขอ:

```json
"ai_providers": {
  "groq": ["key1", "key2", "key3"],
  "gemini": ["key1", "key2"]
}
```

ข้อดี:

* **กระจายโหลด** ข้ามบัญชีหลายบัญชี
* **จัดการข้อจำกัดการเรียก** โดยกระจายคำขอ
* **เพิ่มความเชื่อถือได้** — หากคีย์หนึ่งล้มเหลว จะพยายามใช้คีย์อื่น

---

<details>
<summary><strong>🔑 คีย์ GROQ</strong></summary>

[สร้างคีย์](https://console.groq.com/keys)

<p align="center">
  <img src="https://github.com/MeoMunDep/Discord-Autobot/blob/main/IMAGE/GROQ_API_KEY.png?raw=true" width="600" />
</p>

</details>

<details>
<summary><strong>🔑 คีย์ GROK</strong></summary>

[สร้างคีย์](https://console.x.ai/api-keys)

**หมายเหตุ:** คีย์ Grok มีรูปแบบที่เข้ากันได้กับ OpenAI — รับคีย์จากคอนโซล X.AI

</details>

<details>
<summary><strong>🔑 คีย์ GEMINI</strong></summary>

[สร้างคีย์](https://aistudio.google.com/app/apikey)

<p align="center">
  <img src="https://github.com/MeoMunDep/Discord-Autobot/blob/main/IMAGE/GEMINI_API_KEY.png?raw=true" width="600" />
</p>

</details>

<details>
<summary><strong>🔑 คีย์ OPENROUTER</strong></summary>

[สร้างคีย์](https://openrouter.ai/settings/keys)

<p align="center">
  <img src="https://github.com/MeoMunDep/Discord-Autobot/blob/main/IMAGE/OPENROUTER_API_KEY.png?raw=true" width="600" />
</p>

</details>

<details>
<summary><strong>🔑 คีย์ POE</strong></summary>

[สร้างคีย์](https://poe.com/api_key)

<p align="center">
  <img src="https://github.com/MeoMunDep/Discord-Autobot/blob/main/IMAGE/POE_API_KEY.png?raw=true" width="600" />
</p>

</details>

<details>
<summary><strong>🔑 คีย์ FREEPIK</strong></summary>

[สร้างคีย์](https://api.freepik.com/v1/auth/create)

<p align="center">
  <img src="https://github.com/MeoMunDep/Discord-Autobot/blob/main/IMAGE/FREEPIK_API_KEY.png?raw=true" width="600" />
</p>

</details>

</details>

---

<details>
<summary><strong>🗂️ โครงสร้าง `servers.json` (คู่มือแบบละเอียด)</strong></summary>

ไฟล์ `servers.json` เป็น **หัวใจของการตั้งค่า** ของบอท กำหนดว่า bot จะโต้ตอบกับเซิร์ฟเวอร์ไหนและจะทำอะไรในแต่ละช่อง

---

## 📚 โครงสร้างพื้นฐาน

ไฟล์เป็น **อาร์เรย์ JSON** ที่ประกอบด้วยวัตถุเซิร์ฟเวอร์ แต่ละวัตถุแทนเซิร์ฟเวอร์ Discord ที่คุณต้องการให้บอททำงานด้วย

```json
[
  {
    "name": "Server Name",
    "guild_id": "1234567890123456789",
    "invite_id": "inviteCode",
    "enabled": true,
    "channels": {
      "submit_chat": { ... },
      "raw_chat": { ... },
      "command_chat": { ... },
      "image_chat": { ... }
    }
  }
]
```

---

## 🔑 คำอธิบายฟิลด์

### 1️⃣ **`name`** (String - จำเป็น)

* ชื่อที่อ่านได้สำหรับเซิร์ฟเวอร์ (สำหรับอ้างอิงเท่านั้น)
* ตัวอย่าง: `"Curious"`, `"My Gaming Server"`, `"Crypto Community"`

### 2️⃣ **`guild_id`** (String - จำเป็น)

* ID ของ Discord server
* วิธีเอา: ดูคำแนะนำการคัดลอก ID (Developer Mode)
* ตัวอย่าง: `"1417067849926705235"`

### 3️⃣ **`invite_id`** (String - จำเป็น)

* โค้ดเชิญของเซิร์ฟเวอร์
* ตัวอย่าง: `"altiuslabs"` (จาก discord.gg/altiuslabs)
* ค่าพิเศษ:

  * `""` (สตริงว่าง) - ไม่เข้าร่วมเซิร์ฟเวอร์นี้
  * `"none"` - ไม่เข้าร่วม
  * `"unknown"` - ไม่เข้าร่วม

### 4️⃣ **`enabled`** (Boolean - จำเป็น)

* ควบคุมว่าบอทจะประมวลผลเซิร์ฟเวอร์นี้หรือไม่

  * `true` - ประมวลผลเซิร์ฟเวอร์นี้
  * `false` - ข้ามเซิร์ฟเวอร์นี้

### 5️⃣ **`channels`** (Object - จำเป็น)

กำหนดการกระทำในแต่ละช่อง มี **สี่ประเภท**:

---

## 🗨️ ประเภทช่อง

### 🔗 **`submit_chat`** - ส่งโพสต์ X/Twitter (ใหม่!)

**จุดประสงค์:** ช่องเฉพาะสำหรับส่งลิงก์โพสต์ X/Twitter พร้อมหน่วงเวลาที่ปรับได้

**รูปแบบ:**

```json
"submit_chat": {
  "CHANNEL_ID": {
    "message": "https://x.com/yourpost",
    "delay": [6000, 12000]
  }
}
```

**ฟิลด์:**

* `message` (String): URL ของ X/Twitter ที่จะส่ง
* `delay` (Array): [min, max] คูลดาวน์ (วินาที) ก่อนโพสต์อีกครั้งในช่องนี้
* `enabled` (Boolean, Optional): ตั้งเป็น `false` เพื่อปิดการใช้งานช่องนี้โดยไม่ต้องลบ

**การทำงาน:**

* บอทจะประมวลผลช่องเหล่านี้ใน **โหมด submit-only** หรือ **โหมดปกติ**
* คูลดาวน์แต่ละช่องถูกติดตามแยกกัน
* หาก `submit_x_url: false` ใน configs, ลิงก์ X/Twitter ในช่องปกติจะถูกข้าม

---

### 📝 **`raw_chat`** - ข้อความข้อความธรรมดา

ส่งข้อความข้อความธรรมดาไปยังช่อง

**รูปแบบ (ง่าย):**

```json
"raw_chat": {
  "CHANNEL_ID": "MESSAGE_TO_SEND"
}
```

**รูปแบบ (ขั้นสูง):**

```json
"raw_chat": {
  "CHANNEL_ID": {
    "message": "MESSAGE_TO_SEND",
    "delay": [60, 120],
    "enabled": true
  }
}
```

**ค่าพิเศษ:**

* `"__random_message"` - บอทจะใช้ AI หรือข้อความสุ่มจาก `messages.yaml`
* `"[AI] Your instruction"` - บอทจะใช้ AI ตามคำสั่งที่ให้
* ข้อความปกติ - บอทส่งข้อความนั้นอย่างแม่นยำ (เช่น `"gm"`, `"Hello!"`)

**ตัวอย่าง:**

```json
"raw_chat": {
  "1417068171243815002": {
    "message": "[AI] Respond to random messages",
    "delay": [60, 120]
  },
  "1417084328504197250": {
    "message": "gm",
    "delay": [6000, 12000]
  },
  "9876543210987654321": "__random_message"
}
```

**การทำงาน:**

* ในช่อง `1417068171243815002`: บอทใช้ AI ตามคำสั่ง "Respond to random messages"
* ในช่อง `1417084328504197250`: บอทส่ง "gm" ทุก 6000-12000 วินาที
* ในช่อง `9876543210987654321`: บอทส่งข้อความสุ่มที่สร้างโดย AI

---

### 🤖 **`command_chat`** - คำสั่งบอท

ส่งคำสั่ง slash หรือคำสั่งบอทไปยังช่อง

**รูปแบบ:**

```json
"command_chat": {
  "CHANNEL_ID": {
    "command": "COMMAND_TO_EXECUTE",
    "delay": [60, 120]
  }
}
```

**ตัวอย่าง:**

```json
"command_chat": {
  "1234567890123456789": {
    "command": "/claim daily",
    "delay": [86400, 86500]
  },
  "9876543210987654321": {
    "command": "!rank",
    "delay": [600, 1200]
  }
}
```

---

### 🖼️ **`image_chat`** - ส่งภาพที่สร้างด้วย AI

สร้างและส่งภาพ AI ไปยังช่องโดยใช้ Freepik API (สำรองด้วย Pollinations.ai)

**รูปแบบ:**

```json
"image_chat": {
  "CHANNEL_ID": {
    "prompt": "IMAGE_GENERATION_PROMPT",
    "delay": [60, 120]
  }
}
```

**ตัวอย่าง:**

```json
"image_chat": {
  "1435601397591703675": {
    "prompt": "Airdrop project with 'Curious'",
    "delay": [7200, 10000]
  },
  "9876543210987654321": {
    "prompt": "Beautiful sunset over mountains",
    "delay": [3600, 7200]
  }
}
```

**ข้อกำหนด:**

* ต้องมีคีย์ `freepik` ใน `configs.json`
* หาก Freepik ล้มเหลวหรือไม่มีคีย์ บอทจะใช้ Pollinations.ai อัตโนมัติ (ฟรี)

---

## 📋 ตัวอย่างครบถ้วน

```json
[
  {
    "name": "Curious",
    "guild_id": "1417067849926705235",
    "invite_id": "curious",
    "enabled": true,
    "channels": {
      "submit_chat": {
        "1417080821852147732": {
          "message": "https://x.com/Curious/status/123456",
          "delay": [6000, 12000]
        }
      },
      "raw_chat": {
        "1417068171243815002": {
          "message": "[AI] Respond to random messages",
          "delay": [60, 120]
        },
        "1417084328504197250": {
          "message": "gcurious",
          "delay": [600, 1200]
        }
      },
      "command_chat": {
        "1417068171243815003": {
          "command": "!rank",
          "delay": [600, 1200]
        }
      },
      "image_chat": {
        "1435601397591703675": {
          "prompt": "Airdrop project with 'CURIOUS'",
          "delay": [60, 120]
        }
      }
    }
  },
  {
    "name": "Disabled Server (Not Active)",
    "guild_id": "5555555555555555555",
    "invite_id": "oldserver",
    "enabled": false,
    "channels": {
      "submit_chat": {},
      "raw_chat": {
        "3333333333333333333": {
          "message": "test message",
          "delay": [60, 120]
        }
      },
      "command_chat": {},
      "image_chat": {}
    }
  }
]
```

---

## ⏱️ ระบบหน่วงเวลา (Delay System)

บอทใช้ **ระบบคูลดาวน์อัจฉริยะ** เพื่อป้องกันสแปมและข้อจำกัดการเรียก:

### 📊 ลำดับความสำคัญของหน่วงเวลา (จากสูงไปต่ำ):

1. **หน่วงเฉพาะช่อง (channel-specific delay)** (ใน servers.json)

   ```json
   "1234567890": {
     "message": "gm",
     "delay": [300, 600]  ← นี่จะถูกนำมาใช้ก่อน
   }
   ```

2. **หน่วง global delay_each_chat** (ใน configs.json)

   ```json
   "delay_each_chat": [10, 15]  ← ใช้เป็น fallback ถ้าช่องไม่มีการตั้งค่า delay
   ```

### 🔄 วิธีการทำงาน:

* แต่ละช่องมี **ตัวจับเวลา (cooldown)** แยกกัน
* ตัวจับเริ่มทำงานหลังจาก **ส่งข้อความสำเร็จ**
* หากยังอยู่ในคูลดาวน์ บอทจะข้ามช่องนั้นและบันทึกเวลาที่เหลือ
* คูลดาวน์ถูกเก็บอย่างถาวรใน `chat-timestamps.json`

### 💡 เคล็ดลับ:

* ใช้หน่วงเวลานานขึ้น (3600-7200s) สำหรับช่องที่มีกิจกรรมน้อย
* ใช้หน่วงสั้นกว่า (60-300s) สำหรับช่องที่มีกิจกรรมสูง
* ช่อง submit มักใช้ 6000-12000s
* การสร้างภาพควรใช้หน่วงนานขึ้น (7200-10000s) เพื่อประหยัดค่า API

---

## 💡 เคล็ดลับ & แนวปฏิบัติที่ดี

### ✅ ควรทำ:

* ใช้ชื่อตัวเซิร์ฟเวอร์ที่บอกความหมายเพื่อระบุได้ง่าย
* ตั้ง `enabled: false` เพื่อปิดเซิร์ฟเวอร์ชั่วคราวโดยไม่ลบการตั้งค่า
* ตั้ง `enabled: false` บนช่องเฉพาะเพื่อปิดช่องนั้นโดยไม่ต้องลบ
* ใช้ delay เฉพาะช่องเพื่อการควบคุมที่ดีขึ้น
* ใช้รูปแบบ `"[AI] instruction"` สำหรับการตอบด้วย AI ตามคำสั่งของคุณ
* ใช้ `submit_chat` สำหรับ URL ของ X/Twitter แทนช่องปกติ
* ทดสอบกับเซิร์ฟเวอร์เดียวก่อนเพิ่มหลายเซิร์ฟเวอร์
* ใช้หน่วงเวลายาวสำหรับการสร้างภาพเพื่อลดค่าใช้จ่าย API

### ❌ ห้ามทำ:

* อย่าลบรายการเซิร์ฟเวอร์ — ให้ใช้ `enabled: false` แทน
* อย่าลืมใส่ delay ให้ช่อง (บอทจะใช้ค่าเริ่มต้นได้ แต่ควรระบุให้ชัดเจน)
* อย่าใช้ delay สั้นมาก (< 60s) เพื่อหลีกเลี่ยงข้อจำกัดหรือการถูกแบน
* อย่าใส่ไฟล์ `tokens.txt` ให้คนอื่นดู พกเก็บให้ปลอดภัย
* อย่าแชร์ `tokens.txt` กับใคร
* หลีกเลี่ยงการตั้งค่าที่ผิดพลาดของ JSON — ใช้ validator หากไม่แน่ใจ

---

## 🔍 การหาช่องไอดี (Channel IDs)

1. เปิด Developer Mode ใน Discord (Settings → Advanced → Developer Mode)
2. คลิกขวาที่ช่องที่ต้องการ
3. เลือก "Copy Channel ID"
4. วาง ID ลงใน `servers.json`

[คู่มือภาพ](https://t.me/KeoAirDropFreeNee/1676)

---

## 🐛 การแก้ปัญหา (Troubleshooting)

| ปัญหา                     | วิธีแก้                                                                              |
| ------------------------- | ------------------------------------------------------------------------------------ |
| บอทข้ามเซิร์ฟเวอร์        | ตรวจสอบว่า `enabled: true` ถูกตั้งค่าในเซิร์ฟเวอร์นั้น                               |
| บอทข้ามช่องเฉพาะ          | ตรวจสอบว่าช่องไม่ได้ตั้ง `enabled: false`                                            |
| บอทไม่เข้าร่วมเซิร์ฟเวอร์ | ตรวจสอบว่า `invite_id` ถูกต้อง และ `join_server: true` ใน configs                    |
| บอทไม่ส่งข้อความ          | ตรวจสอบว่า channel IDs ถูกต้อง และ `auto_chat: true` ใน configs                      |
| ข้อความถูกส่งถี่เกินไป    | เพิ่มหรือขยาย `delay` ในการตั้งค่าช่อง                                               |
| ภาพไม่ถูกสร้าง            | ตรวจสอบคีย์ Freepik ใน `configs.json` (หากไม่มี จะใช้ Pollinations.ai เป็น fallback) |
| ช่อง submit ใช้งานไม่ได้  | ตรวจสอบว่าโหมดที่รันตรงกับ submit-only หรือ normal                                   |
| ข้อผิดพลาด JSON           | ใช้ JSON validator เช่น jsonlint.com                                                 |

</details>

---

<details>
<summary><strong>▶️ การใช้งาน</strong></summary>

### 📌 รันบอท

**การใช้งานพื้นฐาน:**

```bash
node index_meomundep.js
```

**ด้วยแฟล็ก CLI:**

```bash
# บังคับเป็นโหมด submit-only (เฉพาะ submit_chat)
node index_meomundep.js --submit
node index_meomundep.js -s

# บังคับเป็นโหมด normal (ไหลงานเต็ม: join, chat, leave)
node index_meomundep.js --chat
node index_meomundep.js -c

# แสดงช่วยเหลือ
node index_meomundep.js --help
node index_meomundep.js -h
```

### 🎮 โหมดการทำงาน

บอทรองรับ **สองโหมดการทำงาน**:

#### 1️⃣ **โหมดปกติ (Normal Mode)** — ไหลงานเต็ม

* เข้าร่วมเซิร์ฟเวอร์ (ถ้า `join_server: true`)
* ประมวลผลทุกประเภทช่อง: `submit_chat`, `raw_chat`, `command_chat`, `image_chat`
* ออกจากเซิร์ฟเวอร์ (ถ้า `leave_server: true`)
* ใช้ AI ในการสร้างข้อความ

**เปิดใช้ด้วย:**

* CLI: `node index_meomundep.js --chat`
* Config: `"auto_chat": true` (ค่าเริ่มต้น)
* ขณะรัน: เลือกตัวเลือก `2` ถ้าถูกถาม (เมื่อ `ask_before_auto_chat: true`)

#### 2️⃣ **โหมดส่งเฉพาะ (Submit-Only Mode)**

* ข้ามการเข้าร่วม/ออกเซิร์ฟเวอร์
* **ประมวลผลเฉพาะ** `submit_chat` เท่านั้น
* เหมาะสำหรับงานส่ง URL ของ X/Twitter โดยเฉพาะ

**เปิดใช้ด้วย:**

* CLI: `node index_meomundep.js --submit`
* Config: `"auto_chat": false`
* ขณะรัน: เลือกตัวเลือก `1` ถ้าถูกถาม (เมื่อ `ask_before_auto_chat: true`)

### 🔄 ลำดับความสำคัญของโหมด (จากสูงสุดถึงต่ำสุด):

1. **แฟล็ก CLI** (--submit หรือ --chat)
2. **การถามผู้ใช้** (ถ้า `ask_before_auto_chat: true`)
3. **การตั้งค่าใน configs** (`auto_chat`)

### 📊 การจัดการแบบโต้ตอบ

เมนูจัดการภายใน:

```bash
node index_meomundep.js
# แล้วเลือก "Server Manager" จากเมนูหลัก
```

**ฟีเจอร์การจัดการ:**

🖥️ **เมนูหลัก:**

1. จัดการเซิร์ฟเวอร์ (เปิด/ปิด)
2. จัดการหน่วงเวลา (เพิ่ม/แสดง)
3. จัดการ submit (ลิงก์ X)
4. จัดการ error channels
5. ออก

📡 **การจัดการเซิร์ฟเวอร์:**

* เปิด/ปิดเซิร์ฟเวอร์ทั้งหมดพร้อมกัน
* เปิด/ปิดเซิร์ฟเวอร์เฉพาะ
* เปิดเพียงเซิร์ฟเวอร์เดียว (ปิดอื่นทั้งหมด)
* แสดงภาพรวมสถานะเซิร์ฟเวอร์

⏱️ **การจัดการหน่วงเวลา:**

* เพิ่มค่า default ให้ช่องที่ยังไม่มี delay
* เพิ่ม delay แบบกำหนดเอง
* แสดงสถิติ delay
* แสดงค่า delay ที่ใช้งานอยู่

🔗 **การจัดการ Submit:**

* แสดงรายชื่อ submit channels ทั้งหมด
* เพิ่ม submit channel ใหม่
* แก้ไข URL ของ submit
* ลบ submit channel
* แสดงสถิติ submit

🚫 **การจัดการ Error Channels:**

* ดู error channels แยกตาม token
* ลบ error channel เฉพาะรายการ
* ลบ entry ของ token ทั้งหมด
* ทำความสะอาดบันทึกช่องที่ล้มเหลว

### ⬆️ อัปเดตบอท

```bash
git pull
npm install
```

---

### 🔍 วิธีการทำงานโดยย่อ

1. อ่านการตั้งค่าจาก `configs.json`
2. ตรวจสอบแฟล็ก CLI (`--submit`, `--chat`, `--help`)
3. ถามผู้ใช้ (ถ้า `ask_before_auto_chat: true` และไม่มีแฟล็ก)
4. ตัดสินใจโหมด (submit-only หรือ normal)
5. ล็อกอินด้วยโทเค็นจาก `tokens.txt`
6. ใช้พร็อกซี่ต่อบัญชี (ถ้ามีใน `proxies.txt`)
7. ประมวลผลแต่ละเซิร์ฟเวอร์จาก `servers.json`:

   * ✅ ตรวจสอบว่า `enabled: true`
   * ✅ ตรวจสอบว่าช่องไม่ได้ตั้ง `enabled: false`
   * ✅ ตรวจสอบคูลดาวน์ก่อนส่ง
   * **ในโหมดปกติ:**

     * ➕ เข้าร่วมเซิร์ฟเวอร์ (ถ้า `join_server: true`)
     * 💬 ประมวลผลทุกประเภทช่อง
     * ➖ ออกจากเซิร์ฟเวอร์ (ถ้า `leave_server: true`)
   * **ในโหมด submit-only:**

     * 🔗 ประมวลผลเฉพาะ `submit_chat`
     * ⏭️ ข้าม `raw_chat`, `command_chat`, `image_chat`
8. บันทึกทุกเหตุการณ์พร้อม timestamp และสีใน log
9. บันทึก timestamps เพื่อป้องกันการโพสต์ซ้ำในช่วงคูลดาวน์
10. รอหน่วงเวลาตามที่ตั้งไว้ระหว่างการกระทำ
11. ทำซ้ำอัตโนมัติตาม `time_to_restart_all_accounts`

---

### 📝 บันทึก (Logs) และ Timestamps

* แต่ละบันทึกมี timestamp
* ฟอร์แมต timestamp ตาม `chat_language`
* ใช้สีต่าง ๆ ใน log:

  * 🟢 เขียว - สำเร็จ
  * 🔴 แดง - ข้อผิดพลาด
  * 🟡 เหลือง - คำเตือน
  * 🔵 น้ำเงิน - ข้อมูล
  * 🟣 ม่วง - กำลังประมวลผล
  * 🔶 ส้ม - การกระทำ AI/Provider

---

### 🎙️ รองรับช่องเสียง (Voice)

บอทสามารถเชื่อมต่อและรักษาการอยู่ในช่องเสียงได้:

```json
"voice_chat": {
  "enabled": true,
  "server_id": "1234567890123456789",
  "channel_id": "9876543210987654321",
  "user_id": "1111222233334444555"
}
```

**ฟีเจอร์:**

* เชื่อมต่ออัตโนมัติไปยังช่องเสียง
* กลไก heartbeat เพื่อรักษาการเชื่อมต่อ
* พยายามเชื่อมต่อใหม่อัตโนมัติเมื่อหลุด (สูงสุด 5 ครั้ง พร้อม exponential backoff)
* รองรับ WebRTC
* การตัดการเชื่อมต่ออย่างสะอาด

**ข้อกำหนด:**

* ต้องมีสิทธิ์ในการเข้าร่วมช่องเสียง
* โทเค็นต้องสามารถเข้าถึงเซิร์ฟเวอร์นั้นได้
* `user_id` ต้องตรงกับบัญชีที่โทเค็นเป็นของ

---

### ⚠️ หมายเหตุสำคัญ

* ✅ ตรวจสอบว่าโทเค็นถูกต้องและมีสิทธิ์ที่ต้องการ
* 🌐 ใช้พร็อกซี่เพื่อลดความเสี่ยงการถูกจำกัด/แบน (แนะนำ 1 พร็อกซี่ ต่อ 1 โทเค็น)
* ⏱️ ปรับค่า delay อย่างระมัดระวังเมื่อใช้บัญชีจำนวนมาก
* 📊 ตรวจสอบ log เพื่อปรับการตั้งค่าและแก้ปัญหา
* 🔒 เก็บโทเค็นและ API keys ให้เป็นความลับและปลอดภัย
* 🚫 อย่าแชร์ไฟล์ `tokens.txt`
* 💾 บอทบันทึกข้อผิดพลาดไปยัง `error-channels.json` เพื่อหลีกเลี่ยงการลองซ้ำช่องที่ล้มเหลว
* 🔄 ใช้โหมด submit-only สำหรับงานส่ง URL โดยเฉพาะ
* 🎯 ตั้งคูลดาวน์แต่ละช่องให้เหมาะสมเพื่อหลีกเลี่ยงการตรวจจับสแปม
* 🤖 ผู้ให้บริการ AI จะถูกลองตามลำดับจนกว่าจะสำเร็จ
* 📸 หาก Freepik ล้มเหลว ระบบจะ fallback ไปยังบริการฟรี

</details>

---

## 📞 ติดต่อ & สนับสนุน

* 🛒 **สนับสนุนผู้พัฒนา:** [Link 1](https://t.me/KeoAirDropFreeNe/312/27801) • [Link 2](https://github.com/MeoMunDep/MeoMunDep)
* 💬 **ติดต่อ:** [@MeoMunDep](https://t.me/MeoMunDep)
* 👥 **กลุ่ม:** [เข้าร่วมกลุ่ม](https://t.me/KeoAirDropFreeNe)
* 📢 **Channel:** [เยี่ยมชมช่อง](https://t.me/KeoAirDropFreeNee)

---

⚠️ **คำเตือน**

โปรเจกต์นี้สร้างขึ้นเพื่อเป็น **การฝึกฝนส่วนตัวทางการศึกษา** เท่านั้น
มัน **ไม่ใช่ผลิตภัณฑ์ทางการ** หรือบริการอย่างเป็นทางการของบริษัทใด ๆ ที่อ้างอิงใน repository นี้
ผู้สร้าง **ไม่มีความเกี่ยวข้องหรือการรับรอง** ใด ๆ กับบริษัทหรือแบรนด์ที่อ้างถึง

ซอร์สโค้ดให้มา **ตามสภาพ ("as is")** โดยไม่มีการรับประกันใด ๆ
บางส่วนของซอร์สโค้ดอาจถูกทำให้เป็นโค้ดที่อ่านยาก (obfuscated) เพื่อปกป้องงานวิจัยส่วนตัวและตรรกะเฉพาะที่พัฒนาในระหว่างการเรียนรู้

ใช้งานโปรเจกต์นี้ด้วยความรับผิดชอบและด้วยความเสี่ยงของคุณเอง
**ห้ามแจกจ่าย ขาย หรือใช้เชิงพาณิชย์** ส่วนใดส่วนหนึ่งของรหัสนี้ — ไม่ว่าจะดัดแปลงหรือไม่ก็ตาม

---

<p align="center"><a href="#-discord-automation-bot-by-meomundep">⬆️ กลับไปด้านบน</a></p>
